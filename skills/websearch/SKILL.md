---
name: websearch
description: a tool that can do retrieval and synthesis on data from the web
---

# Web Search Tool
- To invoke the tool first establish the query.
- Queries can be anything a person would type into a browser or search engine (written in natural language)
- To submit the request use the following bash command where the query is put in quotes at the end:
`gemini -p "QUERY GOES HERE"`
